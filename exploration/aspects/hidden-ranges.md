# Hidden Ranges

This aspect covers direct hidden-hand overrides on top of an exact active-hand `MatchState`.

Initial scenario targets:

- replace one player's remaining hand with an exact hand
- replace one player's remaining hand with ordered strength slots
- verify public state outside the overridden hidden hand stays unchanged
- verify the resolved state keeps the original card ids for that hand
