# Joint Ranges

This aspect covers simultaneous hidden-range overrides for more than one player.

Initial scenario targets:

- both players receive compatible exact overrides
- duplicate-card collisions across players are rejected
- joint weighting multiplies branch weights across players
